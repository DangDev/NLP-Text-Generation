on:
  github:
    branches:
      only: main

jobs:

  generateText:
    resources:
      instance-type: C5
    uses: script@v1
    with:
      script: |-
        lscpu
      image: paperspace/transformers-gpu:0.4.0
